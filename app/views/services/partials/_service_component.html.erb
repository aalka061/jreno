<div class="col-lg-4 col-md-6">
  <div class="container">
    <a href="<%= _url  %>">
      <picture>
        <img  class="img-mod" height="726px"  src="<%= asset_path(path) %>" alt="" />
      </picture>
      <h1 class="centered"> <%= title %></h1>
    </a>
  </div>
</div>

<style media="screen">
  .container {
    position: relative;
    text-align: center;
  }
  .centered {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: white;
    font-weight:bold;
    text-shadow: rgba(0, 0, 0, 0.4) 0px 4px 5px;
    font-size: 38px;

  }

  .img-mod {
    max-width: 100%;
  }
</style>
